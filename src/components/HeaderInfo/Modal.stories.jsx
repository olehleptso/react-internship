import React from 'react';
import HeaderInfo from './HeaderInfo';

export default {
  title: 'HeaderInfo',
  component: HeaderInfo,
};

const Template = (args) => <HeaderInfo {...args} />;

export const Info = Template.bind({});

